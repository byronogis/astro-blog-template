---
import { t } from 'i18next'
import { LanguageSelector } from 'astro-i18next/components'

---

<LanguageSelector showFlag={true} title={t('other.language')} class:list={[
  'component_lang-switcher',
  'bg-transparent',
]} />
